// components/HeroSection.js
const HeroSection = () => {
    return (
        <section className="hero">
            <h1>Grill House Bar</h1>
            <p>People who love to eat are always the best people.</p>
            <button>Reserve Now</button>
        </section>
    );
};

export default HeroSection;
